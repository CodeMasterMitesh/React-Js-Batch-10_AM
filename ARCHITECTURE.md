# 🏗️ E-Commerce Platform Architecture

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                     SHOPHUB E-COMMERCE PLATFORM                 │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│                     FRONTEND (React 18 + Vite)                   │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌─────────────┐  ┌─────────────┐  ┌──────────────┐             │
│  │ Public Pages│  │Auth Pages   │  │ Cart/Checkout│             │
│  ├─────────────┤  ├─────────────┤  ├──────────────┤             │
│  │ Home        │  │ Register    │  │ Cart         │             │
│  │ Shop        │  │ Login       │  │ Checkout     │             │
│  │ Product     │  │             │  │ Order Review │             │
│  │ Blog        │  │             │  │              │             │
│  │ Contact     │  │             │  │              │             │
│  │ About       │  │             │  │              │             │
│  └─────────────┘  └─────────────┘  └──────────────┘             │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │               Global State Management                    │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │ ┌──────────────────┐         ┌──────────────────────┐   │   │
│  │ │  AuthContext     │         │   CartContext        │   │   │
│  │ ├──────────────────┤         ├──────────────────────┤   │   │
│  │ │ • user (user)    │         │ • cart (items[])     │   │   │
│  │ │ • token (JWT)    │         │ • totalPrice         │   │   │
│  │ │ • loading        │         │ • totalItems         │   │   │
│  │ │ • login()        │         │ • addToCart()        │   │   │
│  │ │ • logout()       │         │ • removeFromCart()   │   │   │
│  │ │ • register()     │         │ • updateQuantity()   │   │   │
│  │ └──────────────────┘         │ • clearCart()        │   │   │
│  │                              │ • fetchCart()        │   │   │
│  │                              └──────────────────────┘   │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │            API Layer (Axios)                            │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │ • Base URL: http://localhost:5000/api                   │   │
│  │ • Token Interceptor: Auto-attach JWT                    │   │
│  │ • Error Handler: Centralized error handling             │   │
│  │ • Request Timeout: 5 seconds                            │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
                              ↓↑
                    HTTP/REST API Calls
                              ↓↑
┌──────────────────────────────────────────────────────────────────┐
│              BACKEND (Express.js + Node.js)                      │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌────────────────────────────────────────────────────────┐     │
│  │               Route Handlers                           │     │
│  ├────────────────────────────────────────────────────────┤     │
│  │                                                        │     │
│  │ ┌──────────────┐  ┌──────────────┐  ┌────────────┐  │     │
│  │ │Auth Routes   │  │Product Routes│  │Cart Routes │  │     │
│  │ ├──────────────┤  ├──────────────┤  ├────────────┤  │     │
│  │ │ • register   │  │ • getAll     │  │ • get cart │  │     │
│  │ │ • login      │  │ • getById    │  │ • add      │  │     │
│  │ │ • logout     │  │ • create     │  │ • remove   │  │     │
│  │ │ • getProfile │  │ • update     │  │ • update   │  │     │
│  │ │ • updatePwd  │  │ • delete     │  │ • clear    │  │     │
│  │ └──────────────┘  └──────────────┘  └────────────┘  │     │
│  │                                                        │     │
│  │ ┌──────────────┐  ┌──────────────┐  ┌────────────┐  │     │
│  │ │Order Routes  │  │Customer Route│  │Checkout   │  │     │
│  │ ├──────────────┤  ├──────────────┤  ├────────────┤  │     │
│  │ │ • getAll     │  │ • getProfile │  │ • checkout │  │     │
│  │ │ • getById    │  │ • addAddress │  │ • success  │  │     │
│  │ │ • updateStatus│  │ • getOrders │  │ • failed   │  │     │
│  │ │ • cancel     │  │ • addFav     │  │            │  │     │
│  │ └──────────────┘  └──────────────┘  └────────────┘  │     │
│  │                                                        │     │
│  └────────────────────────────────────────────────────────┘     │
│                         ↓↑                                       │
│  ┌────────────────────────────────────────────────────────┐     │
│  │          Middleware & Controllers                      │     │
│  ├────────────────────────────────────────────────────────┤     │
│  │ • Authentication (JWT verify)                          │     │
│  │ • Authorization (adminOnly, protect)                   │     │
│  │ • Validation (input checking)                          │     │
│  │ • Error Handling (global error handler)                │     │
│  │ • CORS (cross-origin requests)                         │     │
│  └────────────────────────────────────────────────────────┘     │
│                         ↓↑                                       │
│  ┌────────────────────────────────────────────────────────┐     │
│  │         Mongoose Models (10 Collections)               │     │
│  ├────────────────────────────────────────────────────────┤     │
│  │ ┌─────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │     │
│  │ │ User    │ │ Product  │ │ Category │ │ Blog     │  │     │
│  │ └─────────┘ └──────────┘ └──────────┘ └──────────┘  │     │
│  │ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │     │
│  │ │ Order    │ │ Cart     │ │ Payment  │ │Customer  │  │     │
│  │ └──────────┘ └──────────┘ └──────────┘ └──────────┘  │     │
│  │ ┌──────────┐ ┌──────────┐                             │     │
│  │ │Activity  │ │ Website  │                             │     │
│  │ └──────────┘ └──────────┘                             │     │
│  └────────────────────────────────────────────────────────┘     │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
                              ↓↑
                    MongoDB Connection
                              ↓↑
┌──────────────────────────────────────────────────────────────────┐
│                  DATABASE (MongoDB)                              │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              SHOPHUB Database                           │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │                                                          │   │
│  │ Collections:                                            │   │
│  │  • users (authentication)                               │   │
│  │  • products (inventory)                                 │   │
│  │  • categories (product organization)                    │   │
│  │  • blogs (content)                                      │   │
│  │  • carts (shopping carts - guest & user)                │   │
│  │  • orders (customer orders with full details)           │   │
│  │  • payments (payment transactions)                      │   │
│  │  • customers (customer profiles & loyalty)              │   │
│  │  • activitylogs (user activity tracking)                │   │
│  │  • websites (site configuration)                        │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagrams

### User Registration Flow
```
┌──────────┐
│ Register │
│  Page    │
└─────┬────┘
      │ User enters name, email, password
      ↓
┌──────────────────────┐
│ Validation           │
│ • Email format       │
│ • Password strength  │
│ • Unique email check │
└──────┬───────────────┘
       │ Valid data
       ↓
┌──────────────────────┐
│ POST /api/auth/reg   │
└──────┬───────────────┘
       │
       ↓
┌──────────────────────┐
│ Hash Password        │
│ (bcryptjs)           │
└──────┬───────────────┘
       │
       ↓
┌──────────────────────┐
│ Save User to DB      │
│ • name               │
│ • email              │
│ • passwordHash       │
│ • isAdmin: false     │
└──────┬───────────────┘
       │
       ↓
┌──────────────────────┐
│ Return JWT Token     │
│ (httpOnly cookie)    │
└──────┬───────────────┘
       │
       ↓
┌──────────────────────┐
│ Redirect to Login    │
│ or Dashboard         │
└──────────────────────┘
```

### Shopping & Checkout Flow
```
┌────────────────┐
│ Browse Shop    │
│ /shop          │
└────────┬───────┘
         │ Click product
         ↓
┌────────────────────┐
│ Product Detail     │
│ /product/:id       │
└────────┬───────────┘
         │ Add to cart
         ↓
┌────────────────────┐
│ POST /api/cart/add │
└────────┬───────────┘
         │
         ↓
┌────────────────────┐
│ Check if logged in │
└────────┬───────────┘
         │
    ┌────┴────┐
    │          │
  NO│          │YES
    ↓          ↓
┌──────┐  ┌────────────────┐
│Guest │  │Add to user cart│
│Cart  │  │(MongoDB)       │
└──┬───┘  └────┬───────────┘
   │           │
   │ Continue Shopping or Checkout
   │           │
   └─────┬─────┘
         ↓
   ┌──────────────┐
   │ /cart        │
   │ View items   │
   └──────┬───────┘
          │ Proceed to checkout
          ↓
   ┌──────────────────┐
   │ /checkout        │
   │ (Protected route)│
   └──────┬───────────┘
          │ If not logged in
          ↓
   ┌──────────────────┐
   │ Redirect to /login
   └──────┬───────────┘
          │ Login/Register
          ↓
   ┌──────────────────┐
   │ Cart items merge │
   │ with user account│
   └──────┬───────────┘
          │
          ↓
   ┌──────────────────┐
   │ Fill shipping    │
   │ address & select │
   │ payment method   │
   └──────┬───────────┘
          │ Place order
          ↓
   ┌──────────────────────┐
   │ POST /api/checkout   │
   │ Create Order doc     │
   │ Clear cart           │
   └──────┬───────────────┘
          │
          ↓
   ┌──────────────────────┐
   │ Order Confirmation   │
   │ Email (ready)        │
   │ Display Order ID     │
   └──────────────────────┘
```

### Admin Order Management Flow
```
┌─────────────────┐
│ Admin Login     │
│ /admin/login    │
└────────┬────────┘
         │ Admin credentials
         ↓
┌─────────────────────┐
│ Verify JWT & admin  │
│ Set httpOnly cookie │
└────────┬────────────┘
         │
         ↓
┌─────────────────────┐
│ Admin Dashboard     │
│ /admin/dashboard    │
└────────┬────────────┘
         │ View orders
         ↓
┌─────────────────────┐
│ GET /api/orders     │
│ (all orders)        │
└────────┬────────────┘
         │
         ↓
┌─────────────────────┐
│ Display Order List  │
│ • Order ID          │
│ • Customer Name     │
│ • Amount            │
│ • Status            │
└────────┬────────────┘
         │ Click order
         ↓
┌─────────────────────┐
│ Order Details       │
│ GET /api/orders/:id │
└────────┬────────────┘
         │
         ↓
┌─────────────────────┐
│ Update Status       │
│ pending →           │
│ confirmed →         │
│ shipped →           │
│ delivered           │
└────────┬────────────┘
         │ Submit update
         ↓
┌───────────────────────────┐
│ PUT /api/orders/:id/status│
│ Update in DB              │
└──────────┬────────────────┘
           │
           ↓
┌──────────────────────┐
│ Send notification    │
│ (email - when ready) │
└──────┬───────────────┘
       │
       ↓
┌──────────────────────┐
│ Confirm update       │
│ Return to list       │
└──────────────────────┘
```

---

## 🗂️ Component Hierarchy

```
App
├── AuthProvider
│   ├── CartProvider
│   │   ├── ContextProvider
│   │   │   ├── ModalProvider
│   │   │   │   └── Suspense
│   │   │   │       └── RouterProvider
│   │   │   │           ├── AppLayout
│   │   │   │           │   ├── Nav
│   │   │   │           │   ├── Routes (children)
│   │   │   │           │   └── Footer
│   │   │   │           │
│   │   │   │           ├── Auth Routes
│   │   │   │           │   ├── /register (Register)
│   │   │   │           │   └── /login (Login)
│   │   │   │           │
│   │   │   │           ├── Customer Routes
│   │   │   │           │   ├── / (Home)
│   │   │   │           │   ├── /shop (Shop)
│   │   │   │           │   ├── /product/:id (ProductDetail)
│   │   │   │           │   ├── /cart (Cart)
│   │   │   │           │   ├── /checkout (Checkout)
│   │   │   │           │   ├── /account (Account)
│   │   │   │           │   ├── /about (About)
│   │   │   │           │   ├── /blog (Blog)
│   │   │   │           │   ├── /contact (Contact)
│   │   │   │           │   └── /company (Company)
│   │   │   │           │
│   │   │   │           └── Admin Routes
│   │   │   │               ├── /admin/login (AdminLogin)
│   │   │   │               ├── /admin/dashboard (AdminDashboard)
│   │   │   │               ├── /admin/products
│   │   │   │               ├── /admin/categories
│   │   │   │               ├── /admin/blogs
│   │   │   │               ├── /admin/orders
│   │   │   │               └── /admin/settings
```

---

## 🔐 Authentication Flow

```
┌─────────────────────────────────────┐
│ User Action (Login/Register)        │
└────────────────┬────────────────────┘
                 │
                 ↓
      ┌──────────────────────┐
      │ Validate Input       │
      └────────┬─────────────┘
               │
               ↓
      ┌──────────────────────┐
      │ POST to Auth Endpoint│
      └────────┬─────────────┘
               │
         ┌─────┴──────┐
      NO│             │YES
        ↓             ↓
    ┌────────┐   ┌──────────────┐
    │ Error  │   │ For Register:│
    │ Return │   │ Hash password│
    └────────┘   │ Save user    │
                 │              │
                 │ For Login:   │
                 │ Check email  │
                 │ Verify pwd   │
                 └────┬─────────┘
                      │
                      ↓
           ┌─────────────────────┐
           │ Generate JWT Token  │
           │ (signed with secret)│
           └────┬────────────────┘
                │
                ↓
      ┌──────────────────────────┐
      │ Set httpOnly Cookie      │
      │ (secure transmission)    │
      └────────┬─────────────────┘
               │
               ↓
      ┌──────────────────────────┐
      │ Return Success Response  │
      │ • Token (in cookie)      │
      │ • User Data              │
      └────────┬─────────────────┘
               │
               ↓
      ┌──────────────────────────┐
      │ Store in AuthContext     │
      │ • user object            │
      │ • loading state          │
      └────────┬─────────────────┘
               │
               ↓
      ┌──────────────────────────┐
      │ Redirect to Protected    │
      │ Route or Dashboard       │
      └──────────────────────────┘
```

---

## 📡 API Communication

```
Frontend (React)
      │
      │ HTTP Request
      │ • Headers: { Authorization: Bearer <token> }
      │ • Body: JSON data
      ↓
Backend (Express)
      │
      ├→ Authentication Middleware
      │  (Verify JWT from cookie)
      │
      ├→ Authorization Middleware
      │  (Check admin status if needed)
      │
      ├→ Validation Middleware
      │  (Validate input data)
      │
      ├→ Route Handler
      │  (Business logic)
      │
      ├→ Mongoose Model
      │  (Database operation)
      │
      └→ MongoDB
         (Persist data)
      │
      │ HTTP Response
      │ • Status Code: 200/201/400/401/403/500
      │ • Body: JSON { success, data, message }
      ↑
Frontend (React)
      │
      ├→ Axios Interceptor
      │  (Check status code)
      │
      ├→ Context/State Update
      │  (Update global state)
      │
      └→ Re-render Component
         (Display data to user)
```

---

## 🗄️ Database Relationships

```
┌─────────┐           ┌──────────┐
│  User   │──1:N──→   │  Cart    │
│  _id    │           │  userId  │
└─────────┘           └──────────┘
    │ 1:N
    │
    ├→ Orders
    │  ├─ user: ObjectId
    │  └─ items: [{product, qty, price}]
    │
    ├→ Customers
    │  ├─ user: ObjectId
    │  ├─ addresses: []
    │  └─ favoriteProducts: [ObjectIds]
    │
    └→ Payments
       ├─ orderId: ObjectId
       └─ userId: ObjectId


┌──────────┐           ┌──────────┐
│ Product  │──1:N──→   │  Order   │
│  _id     │           │ items[]  │
└──────────┘           └──────────┘
    │ 1:N
    │
    ├→ Carts
    │  └─ items: [{product, qty}]
    │
    └→ Customers
       └─ favoriteProducts: []


┌───────────┐          ┌──────────┐
│ Category  │──1:N──→  │ Product  │
│  _id      │          │ category │
└───────────┘          └──────────┘


┌────────────┐
│ Order      │
├────────────┤
│ _id        │
│ user       │──→ User
│ items[]    │──→ Products
│ shipping   │
│ payment    │──→ Payment
│ status     │
│ total      │
└────────────┘
```

---

## 🔄 State Management Flow

```
User Action
    │
    ↓
dispatch action / call context method
    │
    ↓
Context Function
    │
    ├→ Axios API call
    │  │
    │  └→ Backend validation & processing
    │
    ├→ Update Context State
    │  │
    │  └→ { success, data, error, loading }
    │
    ↓
Component Re-render
    │
    └→ Display updated UI
```

---

## 🚀 Deployment Architecture (Ready)

```
┌────────────────────────────────────────┐
│         Client (Vercel/Netlify)       │
│ • React SPA (Optimized build)         │
│ • Automatic deployments                │
└────────────────────────────────────────┘
                    │
         (HTTPS/REST API Calls)
                    │
┌────────────────────────────────────────┐
│     Backend (Railway/Heroku)          │
│ • Node.js + Express                    │
│ • Environment variables (.env)         │
│ • Auto-scaling ready                   │
└────────────────────────────────────────┘
                    │
            (MongoDB Connection)
                    │
┌────────────────────────────────────────┐
│  Database (MongoDB Atlas)             │
│ • Cloud hosted                         │
│ • Auto backups                         │
│ • Scaling ready                        │
└────────────────────────────────────────┘
```

---

## 📊 Performance Architecture

```
Client-Side
├─ Code Splitting (Lazy loading components)
├─ CSS Modules (No global conflicts)
├─ Context API (Efficient re-renders)
└─ Axios Caching (Request optimization)

Server-Side
├─ Pagination (Reduce data transfer)
├─ Indexing (Fast queries)
├─ Validation (Prevent bad data)
└─ Error handling (Graceful failures)

Database
├─ TTL Index (Auto-cleanup logs)
├─ Query optimization (Select specific fields)
├─ Connection pooling (Reuse connections)
└─ Backup strategy (Data safety)
```

---

This architecture ensures:
- ✅ Scalability
- ✅ Security
- ✅ Performance
- ✅ Maintainability
- ✅ User Experience

**Status**: Production-Ready 🚀
